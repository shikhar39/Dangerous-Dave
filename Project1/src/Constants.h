#pragma once

#include <SFML/Graphics.hpp>
namespace Constants {
	const sf::Vector2f UNIT_SIZE(100.0f, 100.0f);
	static const float VIEW_HEIGHT = 1800.0f;
	const float GRAVITY = UNIT_SIZE.y * 9.81f;
 }